# URL of the Onway backend API. Never hardcode this in source -- every
# API call goes through src/api/client.ts, which reads this at build/dev
# time via import.meta.env.VITE_API_URL.
VITE_API_URL=http://localhost:3000
